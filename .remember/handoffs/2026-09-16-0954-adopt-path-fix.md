# Handoff: Desktop adopt-path fix; JCB-AL-ACA34 identified as the gate-2 rehearsal box

**Session date:** 2026-09-16
**Author:** merpuya via Claude
**Scope:** Close the `desktop-adopt-path-dead-end` follow-up (desktop-mcp and desktop-skills stalled when desktop-inference adopted a hand-authored applied entry), after `/resume` on this box showed the bug live. Session ran on **JCB-AL-ACA34** (Desktop 2.110.0 installed), not JCB-AL-AV01 as the 2026-09-11 handoff assumed.

## What shipped

- **`4d58d2c` on `main` (pushed).** `src/engine/desktop.ts` gained `appliedEntry` (the entry `_meta.json` applies, ours or not, with an `ours` flag) and `adoptedEntry` (the adopt predicate desktop-inference had inline). All three desktop artifacts share them; desktop-inference lost ten lines.
- **desktop-mcp** reads managed servers from the applied entry when adopted; `blocked` with an add-in-the-app / rename-to-`boot-slapper` instruction while they are missing; `present` (after the helper step) once the foreign entry carries them. Verify names the app instead of `bs onboard`, and the helper line says onboard writes it once the servers are there. Needs desktop-inference's `baseUrl` as an option; `aca34` profile updated.
- **desktop-skills** reads `deploymentOrganizationUuid` from the applied entry, foreign or not.
- **CLAUDE.md:** follow-up bullet removed; rule added under "Rules that are not obvious from the code".
- **MeCP:** `desktop-adopt-path-dead-end` → COMPLETED with commit link; `phase-3-desktop-cutover` got a `box_2026_09_16` note; new SCOPED `doctor-parity-clean-check-untracked`; vault README Status line updated.

## Verification performed

- TDD: two artifact tests written first and watched fail (desktop-mcp returned the old "creates it earlier in this run"; desktop-skills was blocked from looking under the sentinel org), then green.
- `npm run typecheck` clean; `npm test` **247/247** (32 files; was 245); `npm run build` clean; `tests/parity/skins.test.ts` 1/1.
- Live on this box after rebuild: `bs plan --headless` shows desktop-mcp blocked with the in-app instruction; `bs doctor --headless` reads servers from the applied entry 'Cornell' and no longer sends the owner to an onboard that cannot help. desktop-inference adopts 'Cornell'; gateway lists 189 models.
- **Not green:** `tests/parity/doctor-parity.test.ts` fails here on `claude-config.clean: bash=warn bs=ok` (four untracked handoff notes in `~/.claude`; pre-existing box state, unrelated). Tracked in MeCP.

## Open items / known follow-ups

- **Gate-2 rehearsal, now on this Mac.** State: applied entry 'Cornell' (hand-authored, gateway), 'Default' beside it, no boot-slapper entry or sidecar. Two routes: add `openbrain` and `mecp` as managed servers in the app (Developer → Configure Third-Party Inference… → Connectors → Apply Changes), then `bs onboard --only desktop-mcp,desktop-skills` writes the headers helper and copies the skill; or rename 'Cornell' to `boot-slapper` so the tool takes it over. Quit Desktop before applying. `docs/cutover/gate-2-runbook.md` does not yet describe the adopt route.
- **Doctor parity `claude-config.clean`** disagreement (MeCP `project:boot-slapper/doctor-parity-clean-check-untracked`): decide whether untracked files count as unclean, or ignore/commit the four notes in dotclaude.
- Remaining CLAUDE.md follow-ups audited today by grep; all still open and accurate (chooser key, plugin `-y`, duplicate detected print, TUI indent, mct skipped outcome, bytes readFile, TUI Ctrl+C child, project-memory re-offer, `.ps1` via `powershell -File` unobserved).

## Files of interest

- `src/engine/desktop.ts` — `appliedEntry`, `adoptedEntry`
- `src/artifacts/desktop-mcp.ts`, `src/artifacts/desktop-skills.ts`, `src/artifacts/desktop-inference.ts`
- `src/profiles/aca34.ts` — `desktop-mcp.baseUrl`
- `tests/unit/artifacts/desktop-mcp.test.ts`, `tests/unit/artifacts/desktop-skills.test.ts` — the adopt-path cases
- `CLAUDE.md`

## Next session — suggested starting point

Verify the hostname first (`scutil --get LocalHostName`); handoff box facts are per-machine. On JCB-AL-ACA34: run the gate-2 rehearsal via one of the two routes above, then `bs doctor`. Expect desktop-mcp and desktop-skills green and open-brain-auth waiting on the in-app Connect. Amend the gate-2 runbook with the adopt route once it has been exercised.
