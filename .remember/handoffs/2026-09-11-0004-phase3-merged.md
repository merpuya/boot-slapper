# Handoff: Phase 3 desktop artifacts + cutover staging merged to main

**Session date:** 2026-09-10 → 2026-09-11
**Author:** Alex Kearns-Apuya via Claude
**Scope:** Execute the Phase 3 plan (`docs/superpowers/plans/2026-09-10-boot-slapper-phase3-desktop-cutover.md`) subagent-driven on a worktree branch, review every task, run a whole-branch review plus one fix wave, fast-forward `main`. Session ran on `JCB-AL-AV01` (no Claude Desktop installed), not the S1/S2 spike Mac.

## What shipped

- **`main` fast-forwarded `8efe657` → `0da8e89`** (17 commits, 40 files, +2160/−42); worktree and branch `worktree-phase3-desktop-cutover` removed after merge.
- **Engine** (`8575ba3`, `6feb1ca`, `33cef97`): `Io.fetch` (RealIo `redirect: "manual"`, FakeIo `onFetch`/`fetches`), cross-surface transitive `requires` in `selectArtifacts`, `EnvProbe.desktopVersion`, and `src/engine/desktop.ts` — 3P paths, MSIX-aware install/version probe, managed sources (`plutil -convert json` / `reg query` / linux JSON) with fail-closed `readable`, `desktopRunning`, config-library read/write with unknown-key-preserving `upsertMeta`, sidecar, `cfgGet` v1|v2, per-`Io` memo for the install/managed probes, shared `readJson`/`runningError`.
- **Helper templates** (`d589ec5`, `a55a52d`): `.sh`/`.ps1` credential and headers helpers with input validation and a baked secret account (runtime `USER`→`USERNAME` fallback when omitted); `prereqs` desktop check via `desktopInstall` with the `1.19367.0` floor.
- **Five desktop artifacts** (`a2b8b66`, `2517433`, `160e7db`, `50fb477`, `adb1bb2`, `df1ab29`): `desktop-inference` (config-library entry + credential helper, live `GET /v1/models` verify), `desktop-mcp` (`managedServers` from `gateway.json`, MeCP headers helper, Open Brain oauth, takeover/version gate, plans on a fresh box before the entry exists), `desktop-skills` (Cowork skills-plugin copy + manifest upsert, running guard), `open-brain-auth` (gate + two read-only grant probes), `hosted-connectors` (instructions only).
- **Profile + parity** (`0d4d593`): `aca34` on `code` + `desktop` (12 artifacts), contract allowlist for the Desktop probes with Desktop-present and managed-plist loops, skins parity fixture satisfied for every desktop artifact.
- **Task 7b** (`546f4cb` plan, `e374b14` code): `project-memory` resolves `<git root>\bin\bash.exe` via `git --exec-path` on win32.
- **Cutover staging** (`55e9591`): `docs/cutover/gate-2-runbook.md` (two-pass onboard), `gate-3-note.md`, `staged/{bootstrap.sh, dotfiles-install-step3.sh|.ps1, gateway-sessions.md}`, `tests/unit/cutover.test.ts`. Cutover state unchanged — `bootstrap.sh` / `claude-gw.zsh` still live.
- **Docs** (`20ecd0d`, `0da8e89`): README desktop section, spec §4 rows 8–12 / §2 / §5 / §6 / §7 amendments, CLAUDE.md rules + follow-ups, plan Deviations §8–§9.

## Verification performed

- Merged `main` at `0da8e89`: `npm run typecheck` clean; `npm test` **245/245** (32 files; baseline 143). Ran on this Mac after `npm ci` in the main checkout (2026-09-11 00:03 EDT).
- On the branch before merge (fix-wave implementer, 2026-09-10 ~23:40): `npm run test:parity` **2/2** (doctor-parity live, skins); `npm run build` clean.
- `node dist/cli.js env | plan --headless | doctor --headless` on this Mac (Task 9): 12 artifacts planned; `desktop-inference`/`-mcp`/`-skills` `blocked: Claude Desktop not installed` (documented reason — Desktop is absent here); `open-brain-auth` absent (no Desktop grant); doctor exit 1 with 14 reds, all pre-existing box state or the Desktop-absent reds. **TUI check not run** (no interactive terminal).
- Reviews: every task reviewed (Tasks 1, 2, 4 needed one fix round each; 3, 5, 6, 7, 7b, 8, 9 clean); whole-branch review (0 Critical, 6 Important) → one fix wave → scoped re-review clean. Ledger + phase report + fix report copied to the session scratchpad (`phase3-sdd/`); the git-ignored SDD workspace went with the worktree.

## Open items / known follow-ups

- **Gate-2 rehearsal (owner's call):** `bs onboard --only desktop-inference,desktop-mcp,desktop-skills` on the Mac with Desktop 1.49585.0 (quit Desktop first; relaunch → third-party option → compare *Export → JSON config* with the written entry). Not runnable here.
- **Adopt-path dead-end** (CLAUDE.md follow-up): when `desktop-inference` adopts a foreign applied entry, `desktop-mcp` never gets an entry and `desktop-skills` uses `ORG_SENTINEL`; needs a distinct blocked reason + reading `deploymentOrganizationUuid` from the applied entry.
- **`.ps1` via `powershell.exe -File`** is an unobserved assumption (plan Deviations §9, CLAUDE.md); fallback is manual in-app entry + a static-credential option.
- **Deferred minors from reviews** (~30, none merge-blocking per the final reviewer): `env.ts`↔`desktop.ts` import cycle; `versionAtLeast` non-numeric segments; `ourEntry` name-only fallback; `readSidecar` unvalidated cast; `FakeIo.onFetch` sync-only; linux `probeManagedSources` lacks the object guard; `desktop-skills` copy never deletes removed files; skins fixture hard-codes `baseUrl`; `sourceHash` null→"null" quirk documented as contract; USAGE `--only desktop` example placement; duplicated contract-loop bodies; `HEAD_PS` duplicates the PasswordVault prelude; coverage gaps (linux branches, 403, `x-api-key`, `newEntryId`). Full list was in the SDD ledger (`phase3-sdd/progress.md` in the session scratchpad — copy into MeCP if wanted).
- `bs doctor` / `test:parity` now need network (live `GET /v1/models`).
- Untracked `.remember/.gitignore` in the main checkout predates this session's work; left alone.
- MeCP `project:boot-slapper/phase-3-desktop-cutover` still IN_PROGRESS — flip on `/save`.

## Files of interest

40 files — see the commits above. Entry points: `src/engine/desktop.ts`, `src/artifacts/desktop-{inference,mcp,skills}.ts`, `src/artifacts/open-brain-auth.ts`, `src/artifacts/hosted-connectors.ts`, `src/artifacts/templates/desktop-helpers.ts`, `src/profiles/aca34.ts`, `tests/unit/artifacts/contract.test.ts`, `tests/parity/skins.test.ts`, `docs/cutover/gate-2-runbook.md`.

## Next session — suggested starting point

`/save` to reconcile MeCP (work item → DONE, decision-log entries for the fail-closed takeover, helper-account baking, and two-pass onboard), then decide the gate-2 rehearsal. On the Desktop Mac: `bs doctor` first (expect `desktop-*` absent, not blocked), then the rehearsal per `docs/cutover/gate-2-runbook.md`.
