# Handoff: boot-slapper Phase 2 complete — PR #2 open (`worktree-phase2-artifacts-tui` → main)

**Session date:** 2026-09-10
**Author:** Alex Kearns-Apuya via Claude
**Scope:** Wrote the Phase 2 plan, then executed it subagent-driven (fresh implementer per task, spec+quality review per task, whole-branch final review + one fix wave) on a worktree branch; opened PR #2.

## What shipped

- **Plan** `docs/superpowers/plans/2026-09-10-boot-slapper-phase2-artifacts-tui.md` (11 tasks, four recorded deviations from the Phase 1 roadmap) — commit `d2ceff1`.
- **Engine touch-ups:** `Prompter.text`, `PlanEntry.opts`, `Artifact.capture?` + `Bundle`, every `check:result` emitted from the run driver, append-only run log, `walkFiles`, shared `buildCtx` in `engine/ctx.ts` — `1dc836e`, `c20bd9c`.
- **Artifacts:** `project-memory` (`4a52704`), `mct` (`61e76dd`; token seeded into `~/.mct/config.json` + bare `mct onboard`, never `--token`), `plugins` (`fae175f`). Profile `aca34` carries all seven `code` artifacts and `mct-sync-token`.
- **`bs capture --out <dir>`:** driver with sha256 manifest, device-bound file refusal, secret scan that refuses the whole bundle — `85be83e`, `454822d`; the `hex-64` pattern now needs a credential word on the same line (`03afa3b`).
- **Ink TUI:** pure reducer + Prompter bridge (`0c56e0c`), Ink 7 / React 19 screens + `runTui` (`c20bd9c`), CLI wiring with `--headless`, TUI by default in a terminal, skins-parity test (`114c440`).
- **Shims:** `install.sh` / `install.ps1` (`1f6d04c`); the curl-pipe entry point hands onboard `/dev/tty`, the PowerShell one never exits the caller's shell (`03afa3b`).
- **Docs:** README, spec amendments (§4 rows 5–7, §5), new `CLAUDE.md` with a Known follow-ups index (`4d55faa`, `03afa3b`).
- **PR #2** https://github.com/merpuya/boot-slapper/pull/2 — 14 commits `d2ceff1..4fd6139`, all SSH-signed.

## Verification performed

On `03afa3b`: `npm run typecheck` clean; `npm test` 143/143 (was 95 at Phase 1); `npm run test:parity` 2/2 — live doctor-parity against `~/.claude/bootstrap.sh --doctor` on JCB-AL-ACA34 (the new `project-memory.*` ids and `mct.status` agree) and the skins-parity test (TUI vs headless onboard leave the fake box identical); `npm run build` clean; `bash -n install.sh` silent; `node dist/cli.js doctor --headless` exits 1 with this Mac's known hooks/gateway-launch drift only. PR #2 CI on `03afa3b`: macOS and Ubuntu green; **windows-latest failed** — the runner checks out with autocrlf, so `install.sh` began with a CRLF shebang and the shims test's first assertion failed (the PowerShell parse of `install.ps1` passed on that same job). Fixed in `4fd6139` (`.gitattributes` pins `*.sh` to LF, which also keeps the shim runnable under Git Bash on a real Windows box); the re-run's outcome is recorded on PR #2.

Not verified this session: the Ink Doctor screen in a real terminal (`npx tsx src/cli.ts doctor`) — no TTY was available; the exit code should match `--headless`.

## Rulings made during execution (each with what it costs if wrong)

1. Implementers ran strictly sequentially; no wave parallelism — wall-clock only.
2. Small per-file helpers duplicated across artifact files accepted — a few duplicated lines.
3. Ink fake streams: escalate rather than switch to `ink-testing-library` — a fix round.
4. Task 3: `mct` validates an existing config before the soft skips — none functionally.
5. Task 8: kept `waitUntilRenderFlush()`; `runTui` resolves only after Ink fully exits; the test helper skips Ink's teardown-only frames — one implementer round.
6. Task 9: the `FakeStdin` fix (queue writes, re-emit on a late listener) accepted as in scope — a helper revert.
7. Final review: curl-pipe shim gets `/dev/tty`; PowerShell shim throws instead of `exit`; `hex-64` scan gated on a credential word, no `--allow` flag — a bare-hex secret with no context word could slip into a bundle.
8. `claude plugin install` stays without `-y` — one manual install if a listed plugin ever declares an install command.

## Open items / known follow-ups

- **Merge PR #2** once CI is green; then run `bs plan` and, as agreed, `bs onboard --only plugins,project-memory` on this Mac (both idempotent, adopt-never-clobber). Everything else waits for the new boxes (gate 2).
- **Real-terminal check** of the Ink Doctor screen (above).
- **`CLAUDE.md` Known follow-ups** carries the rest: deferred engine cleanups (`State.facts`, cross-surface `requires`, tests typecheck, …), UTF-8-only capture, interactive `project-memory` re-offering declined dirs, Windows Git-Bash resolution for the memory sync, TUI Ctrl+C orphaning children, the plugins `-y` policy, `mct` ✓-on-soft-skip.
- Deferred Minor review findings (all non-blocking) live in the merged final-review triage: missing tests for the headless check-group header transition, mct merge-over-existing-config, capture empty profile and mid-bundle write failure, TUI `text`/`gate` prompt kinds; `Object.hasOwn` over `in` on parsed JSON; `walkFiles` follows symlinks; dead `profile ?? aca34` in cli.ts.
- Two commits carry a `Co-Authored-By: Claude Sonnet 5` trailer (the implementer's own attribution) instead of the session's; cosmetic.

## Files of interest

`src/artifacts/{project-memory,mct,plugins}.ts`, `src/engine/{capture,ctx,walk}.ts`, `src/ui/tui/{model,prompter}.ts`, `src/ui/tui/{App,PromptLine,components,index}.tsx`, `src/cli.ts`, `install.sh`, `install.ps1`, `CLAUDE.md`, `tests/parity/skins.test.ts`, `tests/unit/ui/streams.ts`.

## Next session — suggested starting point (any device)

1. If PR #2 is merged: `git pull` on main, `npm ci && npm test` (expect 143/143), then on this Mac `npx tsx src/cli.ts plan --headless` and, if the plan matches expectations, `npx tsx src/cli.ts onboard --only plugins,project-memory`.
2. If not merged: `gh pr checks 2`, fix anything red on the branch in `.claude/worktrees/phase2-artifacts-tui`.
3. Phase 3 plan next: spikes S1/S2, the five desktop artifacts, gates 2–3 — roadmap at the end of the Phase 1 plan; MeCP `project:boot-slapper/phase-3-desktop-cutover` is SCOPED.
