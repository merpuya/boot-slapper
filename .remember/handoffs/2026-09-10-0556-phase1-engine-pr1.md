# Handoff: boot-slapper Phase 1 — engine, four artifacts, headless `bs` CLI, doctor-parity gate 1 (PR #1)

**Session date:** 2026-09-09 → 2026-09-10
**Author:** Alex Kearns-Apuya via Claude
**Scope:** Brainstorm → spec → plan → subagent-driven build of Phase 1 of boot-slapper, the node successor to `dotclaude/bootstrap.sh` for gateway-only Claude Code + Desktop on mac and Windows.

## What shipped

- **Spec + plan** — `docs/superpowers/specs/2026-09-09-boot-slapper-design.md` (artifact × environment × profile model; code + desktop surfaces; gateway-only; three cutover gates) and `docs/superpowers/plans/2026-09-09-boot-slapper-phase1-engine.md` (13 tasks, Phase 2/3 roadmap + backlog). Commits `ca4fd89`, `e7ed9b8` on `main`.
- **Engine** — `src/engine/`: Io + recording fake, env probe with target-OS path joins, Artifact/Ctx/Step/Check + DAG resolver, plan resolver + run driver (blocked/failed/skipped/unchanged; safe detect/plan/verify; exec timeouts), SecretStore (Keychain via hex `-X` on stdin, PasswordVault script on stdin, file fallback; safe-name guard), byte-identical ports of both settings appliers incl. win32 exec-form render.
- **Artifacts** — `prereqs`, `claude-config` (clone/adopt/pull, settings seed, hooks render), `secrets`, `gateway-launch` (generated zsh + PowerShell wrappers, `-File` runner + cmd shim).
- **CLI** — `bs env | plan | doctor [--json] | onboard [--auto] | secrets set|check`; headless reporter; JSONL run log. Profile `aca34` (code surface).
- **Gate 1 passed live** — `tests/parity/`: `bs doctor --json` agrees with `~/.claude/bootstrap.sh --doctor` on JCB-AL-AV01, including the two real non-green rows (hooks drift, no MeCP API key).
- **PR #1** — https://github.com/merpuya/boot-slapper/pull/1 (`phase1-engine` → `main`, 32 commits, head `abdc6d4`); public repo created this session. Worktree kept at `.worktrees/phase1-engine`.
- **Persistence** — MeCP: `projects/boot-slapper`, 4 work items, `decision-log/bootstrap-sh-superseded-by-boot-slapper`, note on `dotclaude-node-cli-unification`; vault READMEs for boot-slapper (new) and dotclaude (pointer); 2 Open Brain thoughts.

## Verification performed

On head `abdc6d4`: `npm run typecheck` clean; `npm test` 95/95 (13 files); `npm run test:parity` 1/1 live; `npm run build` clean. Every task had a task-scoped review (9 of 13 needed one fix round — all findings plan-inherited); whole-branch review (opus) → one fix wave (9 commits) → scoped re-review clean. Nothing was written to `~/.claude`, `~/.zshrc`, `~/.config`, or the Keychain (one throwaway Keychain item created and deleted in the Task 4 smoke). `bs onboard` has NOT been run on any real machine.

## Open items / known follow-ups

- **Merge PR #1** — after your own read; then Phase 2 gets its own plan file.
- **Windows is text-verified only** — PasswordVault get/set semantics (incl. the `$ErrorActionPreference='Stop'` guard), `$PROFILE` pinned to Windows PowerShell 5.1 (pwsh profile is an open question), cmd shim → `-File` runner, entry-point guard from source. All are Phase 3 gate-2 items for the first Windows box.
- **Gate 2 cutover not started** — `bs onboard` must go green on the new mac + Windows box before `bootstrap.sh` becomes a shim, dotfiles hands off, and `claude-gw.zsh` is removed. Until then the bash stays live. Do not run `bs onboard` or `bs secrets set` on this mac.
- **Phase 2 backlog** (end of the plan): `State.facts` channel, `PlanEntry.opts`, cross-surface `requires`, tests typecheck, run-log append/chain guard, `_writeToOutput` guard, non-44 exit codes, `doctor --json` redaction, `BS_REQUIRE_PARITY`, `homeRel` boundary, clone/adopt git timeouts.
- **Spec vs shipped signature** — artifacts take a `Ctx` bundle, not the spec's `detect(env)`/`apply(env, steps, io)`; `mecp-api-key` is a 4th secret service (spec §3 updated in T13).
- **This mac has real hooks drift** (`claude-config.hooks-match` error, bash agrees) — a dotclaude reconcile, not a boot-slapper bug.

## Files of interest

`src/engine/{io,env,artifact,events,plan,run,settings}.ts`, `src/engine/secrets/{store,ref,keychain,passwordvault,filestore}.ts`, `src/artifacts/{prereqs,claude-config,secrets,gateway-launch}.ts`, `src/artifacts/templates/claude-gw.{zsh,ps1}.ts`, `src/profiles/aca34.ts`, `src/ui/{headless,prompt}.ts`, `src/cli.ts`, `tests/unit/**`, `tests/parity/**`, `.github/workflows/ci.yml`, `README.md`.

## Next session — suggested starting point

Review and merge PR #1 (`gh pr view 1`), then `git worktree remove .worktrees/phase1-engine && git branch -d phase1-engine`. Start Phase 2 by writing its plan from the roadmap section at the end of the Phase 1 plan — `project-memory` first (port of claude-memory-sync/install.sh), since gate 2 on a fresh box needs it.
